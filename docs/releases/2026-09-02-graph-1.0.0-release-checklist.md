# Bluetape4k Graph 1.0.0 배포 체크리스트

## 고정 범위

| 항목 | 값 |
|---|---|
| 대상 repository | `bluetape4k/bluetape4k-graph` |
| 대상 버전 | `1.0.0` |
| 직전 공개 버전 | `0.6.0` |
| 통합 기준 | `develop` -> `main` |
| 배포 tag | signed annotated tag `1.0.0` |
| release 준비 PR | [#601](https://github.com/bluetape4k/bluetape4k-graph/pull/601) |
| central catalog source | `bluetape4k-dependencies@791288630092ab17d23ba106c93c1f615b13f02a` |
| 공개 대상 | `io.github.bluetape4k.graph` group의 Graph BOM과 publication registry 전체 artifact |
| 후속 consumer | `bluetape4k-dependencies`의 Graph stable 승격 및 Graph를 소비하는 downstream repository |

## Dispatch hold

현재 상태는 `HOLD`다. 다음 조건이 모두 충족되기 전에는 tag를 만들거나 release
workflow를 실행하지 않는다.

- [ ] PR #601 exact-head CI가 terminal `success`다.
- [ ] PR #601이 exact head를 보존해 `develop`에 병합됐다.
- [ ] 병합 SHA의 Full Nightly `workflow_dispatch(scope=full)`가 terminal `success`다.
- [ ] Full Nightly의 `Testcontainers Image Family Gate`와 `Nightly Status`가 모두
  `completed/success`다.
- [ ] `main`, release tag, Full Nightly가 같은 source commit을 가리킨다.

## 사전 검증

- [x] `baseVersion=1.0.0`, 빈 `snapshotVersion`을 확인했다.
- [x] 직전 GitHub Release가 `0.6.0`임을 확인했다.
- [x] Projects 2.0.0 공개 artifact를 포함하는 immutable catalog commit을 확인했다.
- [x] CHANGELOG에 날짜가 지정된 `1.0.0` section을 준비했다.
- [x] EN/KO README의 Bluetape4k 의존 버전을 `2.0.0`으로 정렬했다.
- [x] publication POM 생성과 Maven effective-model 검증이 성공한다.
- [ ] PR #601 exact-head CI 결과와 merge commit을 기록한다.

## 배포 실행

- [ ] 검증된 `develop` merge SHA를 `main`에 반영한다.
- [ ] 같은 commit에 signed annotated tag `1.0.0`을 만들고 push한다.
- [ ] tag push가 시작한 `Publish Release` workflow의 입력과 checkout SHA를 확인한다.
- [ ] `Verify exact-head Full Nightly`가 사전 검증 run을 재사용한다.
- [ ] `Publish RELEASE to Maven Central Portal`이 성공한다.
- [ ] `Create GitHub Release`가 `1.0.0` CHANGELOG section으로 release notes를 만든다.

## 공개 검증과 후속 조치

- [ ] Maven Central에서 Graph BOM과 publication registry 전체 artifact의 POM/JAR를 확인한다.
- [ ] GitHub tag, Release, Maven Central version이 모두 `1.0.0`으로 일치한다.
- [ ] 이 체크리스트에 exact SHA와 Actions run URL을 기록한다.
- [ ] issue #600과 #603이 PR #601로 닫혔는지 확인한다.
- [ ] `1.0.0` milestone의 열린 issue가 0개인지 확인하고 milestone을 닫는다.
- [ ] `bluetape4k-dependencies`에서 Graph version을 `1.0.0`으로 승격한 뒤 다음
  downstream release wave를 시작한다.
- [ ] 배포와 무관한 CI 최적화는 #602에서 처리한다.

## 증거

- Dependencies catalog commit:
  <https://github.com/bluetape4k/bluetape4k-dependencies/commit/791288630092ab17d23ba106c93c1f615b13f02a>
- Projects 2.0.0 release:
  <https://github.com/bluetape4k/bluetape4k-projects/releases/tag/2.0.0>
- Graph release 준비 PR:
  <https://github.com/bluetape4k/bluetape4k-graph/pull/601>
- Local catalog resolution: `./gradlew help`, `BUILD SUCCESSFUL in 8s`
- Local benchmark/analysis: `:graph-benchmark:test` 28/28, failure/error/skip 0,
  root `detekt` 성공
- Local publication contract: POM 16개, dependency entry 1,825개, Maven
  effective model 16개 검증 성공
- Workflow policy: Python unit test 14/14 및 `actionlint` 성공

## DoD Status

- 상태: `PENDING`
- 완료: target/version/catalog/consumer scope 고정, dispatch hold 정의, 문서 준비,
  local catalog·benchmark·Detekt·publication POM·workflow policy 검증
- 미완료: exact-head CI, merge, Full Nightly, tag, publication, 공개 artifact 검증,
  milestone 및 central catalog 후속 처리

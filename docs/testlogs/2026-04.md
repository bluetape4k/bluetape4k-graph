# Test Log — 2026-04

## 2026-04-12 — graph-core 단위 테스트 보강

### 변경 요약
graph-core 모듈에 단위 테스트가 전무한 상태였기 때문에, 핵심 모델·스키마 DSL·유틸 클래스에 대한 단위 테스트 7개 파일(총 59 케이스)을 새로 추가했다.

### 추가된 테스트 파일

| 파일 | 대상 | 케이스 수 |
|------|------|-----------|
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/model/GraphElementIdTest.kt` | `GraphElementId` value class | 5 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/model/GraphVertexTest.kt` | `GraphVertex` data class | 5 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/model/GraphEdgeTest.kt` | `GraphEdge` data class | 5 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/model/GraphPathTest.kt` | `GraphPath` + `PathStep` sealed | 6 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/model/GraphTraversalOptionsTest.kt` | `NeighborOptions`, `PathOptions`, `Direction` | 7 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/schema/VertexLabelTest.kt` | `VertexLabel` DSL | 6 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/schema/EdgeLabelTest.kt` | `EdgeLabel` DSL | 5 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/utils/GraphPropertiesTest.kt` | `GraphProperties` Cypher 직렬화 | 14 |
| `graph/graph-core/src/test/kotlin/io/bluetape4k/graph/GraphExceptionsTest.kt` | 예외 계층 | 6 |

### 실행 결과

```bash
./gradlew :graph-core:test
# BUILD SUCCESSFUL in 2s
# 59 passing (917ms)
```

### 코드 리뷰에서 발견된 이슈 (tests 로 고정만 하고 수정은 별도 PR 권장)

1. **`GraphProperties` vs `AgePropertySerializer` 중복** — `graph-core/.../utils/GraphProperties.kt` 와 `graph-age/.../sql/AgePropertySerializer.kt` 가 거의 동일한 로직. 후자가 `LocalDate`/`LocalDateTime`/`Map` 처리를 추가로 가지고 있어 사실상 graph-core 쪽이 dead code이며 기능적으로도 stale. 통합 권장.
2. **Cypher 문자열 escape 부족** — `"'"` 만 escape 하고 `\`(백슬래시), `\n`, `\r` 등은 그대로 문자열에 포함됨. 테스트에서는 현재 동작을 그대로 고정했으므로, 후속으로 AGE/Neo4j 파서 호환성 확인 필요.
3. **`else` 브랜치의 잠재적 Cypher 인젝션** — `toString()` + 단일 escape 로만 처리. 스키마 타입 외의 객체가 들어올 경우 위험. 스키마 타입 미지원 시 예외를 던지는 방향을 검토.
4. **`serializableUID` 오타** — 모든 모델 클래스가 `const val serializableUID` 를 선언하지만 Java 직렬화 규약상 이름은 `serialVersionUID` 이어야 해서 현재는 단순 상수. 직렬화 호환성을 원한다면 companion object가 아닌 클래스 본체에 `private const val serialVersionUID` 로 두어야 함.
5. **`EdgeLabel` DSL 불일치** — `VertexLabel` 은 `stringList`, `json` 을 제공하지만 `EdgeLabel` 은 없음. 의도가 있다면 주석, 아니면 동기화 필요.

### 후속 작업 제안

- [ ] `graph-age`, `graph-neo4j`, `graph-tinkerpop` 백엔드별 `ParameterBinder` 분리로 Cypher 리터럴 삽입 대신 파라미터 바인딩 사용

## 2026-04-12 — 코드 리뷰 이슈 후속 수정

### 수정한 이슈

1. **`serializableUID` → `serialVersionUID`** (bluetape4k 컨벤션 준수)
   - `graph/graph-core/src/main/kotlin/io/bluetape4k/graph/model/{GraphElementId,GraphVertex,GraphEdge,GraphPath,GraphTraversalOptions}.kt`
   - 모든 companion object 의 상수를 `private const val serialVersionUID: Long = 1L` 로 교체.

2. **`GraphProperties` ↔ `AgePropertySerializer` 중복 제거**
   - `graph/graph-core/.../utils/GraphProperties.kt` 에 `LocalDate`, `LocalDateTime`, `Map<*, *>` 지원 추가.
   - `graph/graph-age/.../sql/AgePropertySerializer.kt` 는 `GraphProperties` 에 위임하는 얇은 별칭으로 축소 (기존 AGE API 는 하위 호환 유지).

3. **Cypher 문자열 escape 강화**
   - `GraphProperties.toCypherValue(String)` 이 이제 백슬래시, 작은따옴표, `\n`, `\r`, `\t` 모두 escape.
   - 백슬래시를 가장 먼저 처리하여 이어지는 escape sequence 중복 방지.

4. **`EdgeLabel` DSL 비대칭 해소**
   - `graph/graph-core/.../schema/EdgeLabel.kt` 에 `stringList(name)` / `json(name)` 추가 → `VertexLabel` 과 동일한 타입 셋 지원.

### 신규 테스트 케이스 (총 +6)

- `GraphPropertiesTest`: `\\`/`\n`/`\r`/`\t` escape, `Map`, 빈 `Map`, `LocalDate`, `LocalDateTime`
- `EdgeLabelTest`: `stringList`/`json` 매핑 검증

### 실행 결과

```bash
./gradlew :graph-core:test :graph-age:test :graph-neo4j:test \
  :graph-memgraph:test :graph-tinkerpop:test \
  :code-graph-examples:test :linkedin-graph-examples:test
```

| 모듈 | 결과 |
|------|------|
| graph-core | 65 passing |
| graph-age | 21 passing |
| graph-neo4j | 42 passing |
| graph-memgraph | 21 passing |
| graph-tinkerpop | 42 passing |
| code-graph-examples | 48 passing |
| linkedin-graph-examples | 40 passing |

모든 통합 테스트 (AGE/Neo4j/Memgraph Testcontainers 포함) 에서 리그레션 없음을 확인했다.

### 참고

- `graph-age` 는 `api(project(":graph-core"))` 의존성을 이미 가지고 있어 `GraphProperties` 재사용에 추가 설정 불필요.
- `AgePropertySerializer` 를 얇은 facade 로 유지한 이유는 내부 `AgeSql` 에서 모든 call-site 를 건드리지 않고 점진 마이그레이션을 가능하게 하기 위함.
